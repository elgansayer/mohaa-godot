# OpenCode Rules

- This repository uses Godot 4 and OpenMoHAA. 
- Builds are orchestrated via `build.sh`.
- Review `AGENTS.md` to see the structure of our AI-first approach.
- For WebAssembly builds, rely on the python scripts in `scripts/web_assets/` for HTML/JS patching.
